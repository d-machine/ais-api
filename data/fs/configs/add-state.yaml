sections:
  - sectionType: FIELDS
    sectionName: State Details
    queryInfo:
      returnType: SINGLE_ROW
      query: |
        SELECT
          sm.id as state_id,
          sm.name as state_name,
          sm.code as state_code,
          sm.country_id as country_id,
          cm.name as country_name,
          descr as descr
        FROM
          wms.state sm
          LEFT JOIN wms.country cm ON sm.country_id = cm.id
        WHERE
          sm.id = $1
      payload:
        - country_id
      path:
        - sections
        - 0
    applicableActions:
      - SAVE
      - CANCEL
    actionConfig:
      SAVE:
        label: Create State
        actionType: EXECUTE_QUERY
        queryInfo:
          returnType: SCALAR
          query: |
            SELECT wms.insert_state($1, $2, $3, $4, $5) as id;
          payload:
            - state_name
            - state_code
            - descr
            - country_id
          contextParams:
            - current_user_id
          path:
            - sections
            - 0
            - actionConfig
            - SAVE
        onSuccess: exitAndComplete
        onFailure: showErrorSnackbar
      CANCEL:
        label: Discard Changes
    fields:
      - name: state_name
        label: State Name
        type: TEXT
        required: true
      - name: state_code
        label: State Code
        type: TEXT
        required: true
      - name: country_id
        label: Country
        type: SELECT
        required: true
        multi: false
        selectConfig:
          resource: list-countries
          selectHandler: converter
          currentSelection:
            - key: country_id
              as: country_id
          selectParser: converter
          fields_to_extract:
            - key: country_id
              as: country_id
            - key: country_name
              as: country_name
      - name: descr
        label: descr
        type: TEXTAREA
