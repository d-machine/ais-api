version: 1.0
configName: list-po-statuses

sections:
  - sectionType: TABLE
    sectionName: po_statuses
    queryInfo:
      returnType: MULTIPLE_ROWS
      query: |
        SELECT code as id, label
        FROM wms.status_code
        WHERE entity = 'PO'
        ORDER BY code
      path:
        - sections
        - 0

    columns:
      - name: id
        label: Code
        visible: false

      - name: label
        label: Status
        visible: true
